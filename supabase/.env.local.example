# =============================================================================
# BaytMiftah — Supabase CLI / database scripts (gitignored)
# Copy to supabase/.env.local
#
#   cp supabase/.env.local.example supabase/.env.local
#
# Used by: npm run db:status | db:push | db:query | supabase:deploy:payments
# =============================================================================

# ── Project ──────────────────────────────────────────────────────────────────
SUPABASE_PROJECT_REF=your-project-ref

# ── Database (for scripts/supabase-db.mjs) ───────────────────────────────────
SUPABASE_DB_PASSWORD=your-database-password
SUPABASE_DB_REGION=eu-west-1
SUPABASE_DB_PORT=5432

# Optional: full connection string overrides the fields above
# SUPABASE_DB_URL=postgresql://postgres.your-ref:password@aws-0-eu-west-1.pooler.supabase.com:5432/postgres

# ── Supabase personal access token (CLI deploy & link) ───────────────────────
# Create at https://supabase.com/dashboard/account/tokens
SUPABASE_ACCESS_TOKEN=your-personal-access-token
